<?= $this->include('layouts/header') ?>
<div class="page-head"><div><div class="page-kicker">Inventory transaction</div><h1 class="page-title"><?=esc($transaction['transaction_no'])?></h1><p class="page-subtitle">Confirmed <?=esc($transaction['type'])?> movement and its complete audit trail.</p></div><div class="head-actions"><?php if(($transaction['type']??'')==='OUT' && ($transaction['status']??'')==='CONFIRMED'): ?><a class="btn primary" href="<?=site_url('inventory/transactions/'.(int)$transaction['id'].'/challan')?>" target="_blank">🖨 Print Challan</a><?php endif; ?><a class="btn" href="<?=site_url('inventory/transactions')?>">← Transactions</a></div></div>
<div class="grid grid-2"><div class="card"><div class="section-head"><div><h2 class="section-title">Transaction details</h2></div></div><div class="detail-grid"><div><span>Type</span><strong><?=esc($transaction['type'])?></strong></div><div><span>Status</span><strong><?=esc($transaction['status'])?></strong></div><div><span>Date</span><strong><?=esc($transaction['created_at'])?></strong></div><div><span>Created by</span><strong><?=esc($transaction['user_name']??'System')?></strong></div><div><span>Reference</span><strong><?=esc($transaction['reference_no']??'—')?></strong></div><div><span>Party / Destination</span><strong><?=esc($transaction['party_name']??'—')?></strong></div><div><span>Vehicle</span><strong><?=esc($transaction['vehicle_no']??'—')?></strong></div><div><span>Remarks</span><strong><?=esc($transaction['remarks']??'—')?></strong></div></div></div><div class="card"><div class="section-head"><div><h2 class="section-title">Print copies</h2><div class="section-meta">A4 sheet · Original + Customer Copy</div></div></div><div class="card-body"><p class="muted">Print the confirmed OUT challan as two copies on one page, separated by a tear/cut line.</p><?php if(($transaction['type']??'')==='OUT' && ($transaction['status']??'')==='CONFIRMED'): ?><a class="btn primary" target="_blank" href="<?=site_url('inventory/transactions/'.(int)$transaction['id'].'/challan')?>">Open print preview</a><?php endif; ?></div></div></div>
<div class="card" style="margin-top:18px"><div class="section-head"><div><h2 class="section-title">Items</h2><div class="section-meta"><?=number_format(count($items))?> line(s)</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>#</th><th>Product</th><th>Variant</th><th>Quantity</th><th>Unit</th></tr></thead><tbody><?php foreach($items as $i=>$item): ?><tr><td><?=$i+1?></td><td><strong><?=esc($item['name'])?></strong><div class="muted mono"><?=esc($item['code'])?></div></td><td><?=esc($item['variant_name']??'Default')?></td><td><strong><?=esc(rtrim(rtrim(number_format((int)$item['quantity']),'0'),'.'))?></strong></td><td><?=esc($item['unit'])?></td></tr><?php endforeach; ?></tbody></table></div></div>
<?= $this->include('layouts/footer') ?>
